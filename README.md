# goBarberApi 

## Backend
Pre-requisites

docker
`sudo apt install docker &&  apt install docker-compose` 

To Up the docker 
`sudo systemctl start docker` 
`sudo docker-compose up` 

Sequelize migration
`yarn migration:run`

## Frontend